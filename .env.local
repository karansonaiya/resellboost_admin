# PayPal public client id only. NEVER put the PayPal secret here - any
# VITE_-prefixed value gets bundled into the client-side JS and shipped to
# every visitor's browser. The secret that used to be on this line was
# exposed this way; it must be rotated in the PayPal dashboard and, if ever
# needed again, only stored as a Supabase Edge Function secret
# (`supabase secrets set PAYPAL_SECRET=...`), never here.
VITE_PAYPAL_CLIENT_ID=BAASGWLUsq1XPrvjKNqrWXhR5HQAz7KTFhFUxKP-_35txVyzH1_l9OvcklpkzUyhxdzNsY0UUZFLtrtnVI
